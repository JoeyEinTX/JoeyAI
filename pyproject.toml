[project]
name = "joeyai"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["flask"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["joeyai", "joeyai.backend", "joeyai.backend.routes", "joeyai.backend.services"]
